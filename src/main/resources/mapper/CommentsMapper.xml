<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.corgy.mapper.CommentsMapper">
    <resultMap id="BaseResultMap" type="cn.corgy.entity.CommentInfo">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="comContent" jdbcType="VARCHAR" property="comContent"/>
        <result column="articleId" jdbcType="INTEGER" property="articleId"/>
        <result column="userId" jdbcType="INTEGER" property="userId"/>
        <result column="commentTime" jdbcType="TIMESTAMP" property="commentTime"/>
    </resultMap>
    <sql id="Base_Column_List">
        id
        ,comContent, articleId, userId, commentTime
    </sql>
    <select id="findAll" resultType="cn.corgy.entity.CommentInfo">
        select
        <include refid="Base_Column_List"/>
        from comments
        <where>
            <if test="'null!=comment">
                and context like CONCAT('%',#{comment},'%')"
            </if>
            <if test="'null!=userId">
                and userId=#{userId}
            </if>
            <if test="'null!=articleId">
                and articleId=#{articleId}
            </if>
        </where>
    </select>
    <delete id="deleteById" parameterType="java.lang.Integer">
        delete
        from comments
        where id = #{id,jdbcType=INTEGER}
    </delete>
    <insert id="insertComment"
            keyColumn="id"
            keyProperty="id"
            parameterType="cn.corgy.entity.CommentInfo"
            useGeneratedKeys="true">
        insert into comments (comContent, articleId, userId)
        values (#{comContent,jdbcType=VARCHAR}, #{articleId,jdbcType=INTEGER}, #{userId,jdbcType=INTEGER})
    </insert>

    <update id="updateByComment" parameterType="cn.corgy.entity.CommentInfo">
        update comments
        <set>
            <if test="comContent != null">
                comContent = #{comcontent,jdbcType=VARCHAR},
            </if>
            <if test="articleId != null">
                articleId = #{articleid,jdbcType=INTEGER},
            </if>
            <if test="userid != null">
                userId = #{userid,jdbcType=INTEGER},
            </if>
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>
</mapper>